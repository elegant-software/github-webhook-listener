{
  "name": "project-item-status-webhook",
  "version": "0.1.0",
  "private": true,
  "description": "Webhook receiver that forwards GitHub Projects v2 item status changes as repository dispatch events.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
